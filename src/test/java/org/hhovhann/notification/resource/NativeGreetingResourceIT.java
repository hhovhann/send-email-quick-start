package org.hhovhann.notification.resource;

import io.quarkus.test.junit.NativeImageTest;
import org.hhovhann.notification.resource.MailingResourceTest;

@NativeImageTest
public class NativeGreetingResourceIT extends MailingResourceTest {

    // Execute the same tests but in native mode.
}